import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'examen.progamacion.hibrida',
  appName: 'examen-progamacion-hibrida',
  webDir: 'www'
};

export default config;
